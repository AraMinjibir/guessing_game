# guessing_game
A simple command-line guessing game built while learning Rust from the official documentation. The program generates a random number and prompts the user to guess it, providing feedback (too high, too low) until the correct number is entered. This project demonstrates basic Rust concepts including variables, user input, pattern matching, error handling, and loops.
